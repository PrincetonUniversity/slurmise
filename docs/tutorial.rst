Tutorial
========

This tutorial will walk through how to install and use slurmise
with an included example job. Follow along on a terminal connected
to a SLURM-enabled location like an HPC.

This tutorial installs `slurmise` from the github repo, but for your own `slurmise` usage
you can install `slurmise` to your preference by looking at ??? CLAUDE TODO LINK TO install.rst ???

1. Clone the slurmise repository and install slurmise into a venv
   - git clone git@github.com:PrincetonUniversity/slurmise.git
   - cd slurmise
   - python -m venv .venv
   - source .venv/bin/activate
   - pip install .

2. Check that `slurmise --help` produces a help message
   - For you this might be `uvx slurmise --help` or `pipx slurmise --help`
   - ??? CLAUDE TODO INCLUDE slurmise --help output ???

3. Clone and go into the `tutorial/` folder which has examples to run
   - cd tutorial
   - ls

   ??? CLAUDE TODO include what files ls will show ???

3.
