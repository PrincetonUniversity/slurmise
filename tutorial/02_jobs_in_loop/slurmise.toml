[slurmise]
base_dir = "."

[slurmise.job.perfectScaler]
job_spec = "--intensity {intensity} --duration {duration}"
default_mem = 5000
default_time = 30
[slurmise.job.perfectScaler.variables]
intensity = {type = "numeric"}
duration = {type = "numeric"}
